apiVersion: v1
kind: Pod
metadata:
  labels:
    run: ubuntu-pod
  name: ubuntu-pod
spec:
  containers:
  - image: ubuntu
    name: ubuntu-pod
    command: ['sh', '-c', 'sleep 3d']
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
